"""iOS driver — placeholder until M5+.

Reference: `prior art in the agentic-CLI orchestration space` and
`prior art in the agentic-CLI orchestration space`. iOS lacks a clean Linux/Docker path; the
host driver design is deferred until we have a Mac host story.
"""

from __future__ import annotations
