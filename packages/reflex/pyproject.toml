[project]
name = "selffork-reflex"
version = "0.0.1"
description = "Reflex pillar — Yamaç-style adapter fine-tuning. Skeleton only in MVP v0; implementation lands in M7."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "Yamaç Bezirgan" }]
dependencies = []

[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/selffork_reflex"]
