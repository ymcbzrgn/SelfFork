/**
 * Vitest global setup — adds @testing-library/jest-dom matchers
 * (toBeInTheDocument, toHaveAttribute, ...) on top of vitest's
 * built-in `expect`.
 */
import "@testing-library/jest-dom/vitest";
